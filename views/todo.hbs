<!DOCTYPE html>
<html>
<meta charset="utf-8"/>
<title>To-do</title>
<link rel="stylesheet" href="/styles/todo.css">
    <div class="form-container">
    <h1>To-Do List</h1>
    <form method="post" action="/todo/list/">
        <table>
        {{#each items as |value key|}}
        <tr>
            <td><input name="item{{key}}" type="checkbox" value="done" {{#if this.done}}checked{{/if}}/>
            {{this.name}}
            </td>
        </tr>
        {{/each}}
        {{#if items.length}}
        <br>
        <tr>
            <td colspan="2">
            <input type="submit" value="Save" formaction="/todo/save" class="btn"/>
            <input type="submit" value="Remove Checked" formaction="/todo/remove" class="btn"/>
            </td>
        </tr>
        {{else}}
        <p>No to-do items
        {{/if}}
        </table>
    </form>
    <br>
    <form action="/todo/add/" method="post"> Add new item: 
        <input type="text" placeholder="Enter item description" style="width: 60ex" name="item" autofocus required/>
        <input type="submit" value="Add"/>
    </form>
    </div>
</html>